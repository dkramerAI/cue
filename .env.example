# Enables AI event extraction ("Start with anything"). Optional —
# the manual builder, import, templates, and export work without it.
OPENAI_API_KEY=

# Canonical site URL used for metadata. Optional.
NEXT_PUBLIC_SITE_URL=https://cue.app
